---
page_title: "{{ range $index, $element := (split .Name "_") }}{{ if ne $index 0 }} {{ end }}{{ title $element }}{{ end }} {{.Type}} - {{.ProviderName}}"
{{ if gt (len (split .Name "_")) 2 -}}
subcategory: "{{ index (split .Name "_") 1 | title }}"
{{- else -}} 
subcategory: ""
{{- end }}
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{ range $index, $element := (split .Name "_") }}{{ if ne $index 0 }} {{ end }}{{ title $element }}{{ end }} ({{.Type}})

{{ if gt (len (split .Description " --- ")) 1 -}}
{{ index (split .Description " --- ") 1 | trimspace }}
{{ else }}
{{ .Description | trimspace }}
{{- end }}

{{ if .HasExample -}}
## Example Usage

{{ tffile (printf "%s%s%s" "examples/data-sources/" .Name "/data-source.tf") }}
{{- end }}

{{ .SchemaMarkdown | trimspace }}
{{- if .HasImport }}

## Import

Import is supported using the following syntax:

{{ codefile "shell" (printf "%s%s%s" "examples/data-sources/" .Name "/import.sh") }}
{{- end }}